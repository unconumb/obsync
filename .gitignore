node_modules/
dist/
.env
*.obsync.tmp
# Note: ~/.obsync/ is the user-level state directory (outside this repo)
.obsync/
coverage/
*.tgz

# Personal config with machine-specific paths — never commit
obsync.yml

# Claude Code local settings and worktrees
.claude/

# Tauri widget build artifacts (regenerated by `cargo build` / `tauri dev`/`build`)
widget/src-tauri/target/
widget/src-tauri/gen/
